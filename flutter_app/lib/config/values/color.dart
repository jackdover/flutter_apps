import 'package:flutter/material.dart';
import 'dart:ui';

class AKColor {
  static const Color primaryColor = Colors.blue;
  static final Color canvasColor = Colors.grey[100]; //isDark ? Colors.grey[850] : Colors.grey[50];
  static final Color accentColor = Colors.blue[300]; //Colors.tealAccent[200] : primarySwatch[500];
}
